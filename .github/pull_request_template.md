## What changed & why

<!-- Briefly describe the change and the motivation behind it. -->

- [ ] tests pass locally: `python3 -m unittest discover -s tests -v` (macOS/Linux) or `py -m unittest discover -s tests -v` (Windows)
- [ ] no new dependencies
- [ ] `CHANGELOG.md` has an entry under `## Unreleased` for any user-visible change
- [ ] `README.md` / `DECISIONS.md` updated if a documented behavior or a design decision changed
